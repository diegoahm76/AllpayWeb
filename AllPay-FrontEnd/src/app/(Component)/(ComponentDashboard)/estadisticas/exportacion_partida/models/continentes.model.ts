export interface ContinentesData {
    continentes: string[];
}

export interface ContinentesResponse {
    success: boolean;
    detail: string;
    data: string[];
}

export interface ContinentesParams {
    // Por ahora no hay parámetros específicos, pero se puede extender en el futuro
} 