'use client';

import React from 'react';
import SearchCollectors from '@/app/(Component)/(ComponentDashboard)/recaudadores/consultar_factura/components/CheckInvoices';

const ConsultaFacturas = () => {

    return (
        <div className="w-full">
            <SearchCollectors />
        </div>
    );
};

export default ConsultaFacturas;
