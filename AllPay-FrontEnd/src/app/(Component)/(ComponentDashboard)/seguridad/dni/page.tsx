'use client';

import React from 'react';
import DniTable from '@/app/(Component)/(ComponentDashboard)/seguridad/dni/components/DniTable';

const DniPage: React.FC = () => {
    return <DniTable />;
};

export default DniPage; 