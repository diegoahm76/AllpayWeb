export interface ViasData {
    vias: string[];
}

export interface ViasResponse {
    success: boolean;
    detail: string;
    data: string[];
}

export interface ViasParams {
    // Por ahora no hay parámetros específicos, pero se puede extender en el futuro
} 